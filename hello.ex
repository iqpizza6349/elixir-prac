# 'Hello, World'라는 문자열을 IO 를 통해 출력
IO.puts "Hello, World!";

# elixir 는 기본적으로 snake_case 를 사용한다.
cheese_cake = "Cheesy and tasty!";

IO.puts cheese_cake;

# 예약어는 다음과 같다.
# 참고로 elixir 는 기본적으로 여러 줄 주석을 제공하고 있지 않다고 한다.
# 대신, @doc 혹은 @moduledoc 을 사용하면 가능하다고 한다.
# after, and, catch, do, inbits, inlist, nil, else, end, not,
# or, false, fn, in, rescue, true, when, xor, __MODULE__,
# __FILE__, __DIR__, __ENV__, __CALLER__
# 뭔가 파이썬과 JS를 많이 닮은 꼴이다.
